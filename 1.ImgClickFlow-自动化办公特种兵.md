
```markdown
---
# ImgClickFlow 完全上手指南 —— 从零基础到高级特性

本文档专注于 **ImgClickFlow 自身** 的全部功能。读完本文档，你将能够用 ImgClickFlow 独立完成任何桌面 GUI 自动化任务。


## 零、5 分钟环境准备

```bash
pip install opencv-python numpy Pillow pywin32
```

安装后运行环境诊断：

```python
from imgclickflow import auto
auto.check()  # 检查分辨率、缩放、依赖项
```


## 一、核心概念：模板截图驱动

ImgClickFlow 的核心原理只有一句话：**先截一张小图作为"模板"，再让程序在屏幕上找到它，然后点击**。

```
你的操作                          ImgClickFlow 的工作
─────────                        ────────────────
截图"保存按钮"的小区域    →       在屏幕上搜索这张图
                                找到匹配位置
                                把鼠标移过去
                                单击
```

所有功能都建立在这个基础之上。


## 二、基础操作（10 分钟上手）

### 2.1 单击

```python
from imgclickflow import auto

auto.click("保存")  # 在屏幕上找到"保存"按钮的截图，并点击
```

### 2.2 双击 / 右键

```python
auto.dclick("文件")   # 双击
auto.rclick("桌面")   # 右键点击
```

### 2.3 键盘输入

```python
auto.write("2026年工作报告")   # 输入文本
auto.press("enter")            # 按单个键
auto.hotkey("ctrl", "c")       # 组合键
```

### 2.4 延时等待

```python
auto.delay(1.5)  # 等待1.5秒，给界面反应时间
```

### 2.5 基础案例：另存为文件

```python
from imgclickflow import auto

auto.click("文件菜单")
auto.delay(0.5)
auto.click("另存为")
auto.delay(0.5)
auto.write("2026年5月工作报告")
auto.press("enter")
auto.delay(1)
auto.click("关闭")
```


## 三、链式调用：把流程写成一句话

链式调用是 ImgClickFlow 的标志性写法，把多个操作串成一条清晰的流水线。

### 3.1 基本语法

```python
auto.do()
    .操作1()
    .操作2()
    .操作3()
    .run()
```

### 3.2 案例：新建文档并填写

```python
auto.do()
    .click("新建")
    .delay(0.5)
    .write("每日巡检报告")
    .press("enter")
    .write("日期：2026年5月8日")
    .press("enter")
    .write("设备状态：正常")
    .click("保存")
    .delay(0.5)
    .write("巡检报告_20260508")
    .press("enter")
    .run()
```

### 3.3 链式调用的优势

- 流程自上而下，读代码 = 读流程
- 可以存为纯文本用 Git 版本管理
- 可以包裹在 `if` / `for` / 函数中，获得 Python 的全部能力


## 四、核心点击方法（进阶特性）

### 4.1 序列点击 `click_seq`

依次点击一连串按钮，避免写多个独立的 `click()` + `delay()`。

```python
auto.click_seq(["新建", "填写明细", "保存", "确定"])
```

每个点击之间自动有短暂间隔，适合点击菜单路径或连续弹窗按钮序列。

### 4.2 候选点击 `click_any`

同一功能在不同系统里可能叫"确认"/"OK"/"确定"。`click_any` 会依次尝试列表中的每一个模板，哪个先匹配到就点击哪个，匹配成功后不再尝试后续模板。

```python
auto.click_any(["确认", "OK", "确定"])  # 任意一个出现就点击
```

适用场景：
- 多语言环境的软件
- 不同版本 UI 文案不一致
- 需要兼容多种可能的弹窗按钮

### 4.3 鲁棒点击 `click_robust`

这是 ImgClickFlow 的招牌功能。在系统卡顿、按钮轻微位移时，普通 `click` 如果第一次找不到目标就会失败，而 `click_robust` 会在首次找到的位置附近局部重新搜索并点击，同时避免全屏重搜导致误点到其他相同的按钮。

```python
auto.click_robust("保存", retries=3, interval=0.8)
```

参数说明：
- `retries`：最多重试次数（默认 2-3）
- `interval`：重试间隔秒数（**默认 0.6s**；因为 0.5s 以内可能被系统识别为双击，请勿设置低于 0.6 秒的值）
- `search_radius`：局部搜索半径（默认 10 像素）

适用场景：
- 系统繁忙时按钮响应慢
- 网络延迟导致 UI 渲染不稳定
- 按钮在局部区域内轻微位移

**与普通 `click` 的区别**：`click` 找不到就报错，`click_robust` 在局部重试，既提升了成功率，又避免了全屏误点。

案例：提交审批单

```python
auto.do()
    .click("新建审批单")
    .write("设备采购申请")
    .click("提交审批")
    .click_robust("确认提交", retries=3, interval=0.8)
    .wait("审批已提交成功")
    .click("关闭")
    .run()
```

### 4.4 多次连击 `click_multi`

需要连续点击同一个位置多次（如数量加号、计数器增加）。

```python
auto.click_multi("加号", times=5, interval=0.2)  # 点击加号5次，间隔0.2秒
```

适用场景：
- 数量选择器的"+"按钮
- 滚动条微调
- 需要快速连续点击的按钮

### 4.5 坐标点击

直接用屏幕坐标点击（无需模板截图）。

```python
auto.click(500, 300)     # 点击坐标(500, 300)
auto.dclick(500, 300)    # 双击
auto.rclick(500, 300)    # 右键
```

### 4.6 区域限定搜索 `rect`

限定搜索范围，避免全屏匹配到错误目标。

```python
auto.click("按钮", rect=(0, 0, 800, 600))  # 只在左上角800x600区域内搜索
```

适用场景：
- 屏幕上有多个相同外观的按钮，但只需要点某个区域的
- 提高搜索速度

### 4.7 拖拽与滚轮

```python
auto.drag(100, 200, 500, 200)     # 从(100,200)拖到(500,200)
auto.scroll("up", clicks=3)        # 向上滚3格
auto.scroll("down", clicks=5)      # 向下滚5格
```


## 五、同步机制：等待与检测

### 5.1 等待图像出现 `wait`

比 `delay()` 更可靠的方式——不是盲等时间，而是等某个图像真的出现。

```python
auto.wait("保存成功提示", timeout=10)  # 等最多10秒，提前出现就提前继续
```

### 5.2 等待图像消失 `wait_not`

等待加载动画或遮罩消失后再继续。

```python
auto.wait_not("加载中", timeout=30)  # 等"加载中"消失
```

### 5.3 `wait` vs `delay` 的选择

- 时间已知且稳定 → `delay()`
- 时间不确定、受网络/系统影响 → `wait()` / `wait_not()`


## 六、流程控制（中级特性）

### 6.1 循环 `for_data`

```python
data = ["传感器A", "继电器B", "采集器C"]

auto.do()
    .click("新增录入")
    .for_data(data)
        .click("名称输入框")
        .write("{item}")           # {item} 是占位符，依次替换为列表中的元素
        .click("保存")
        .wait("保存成功")
        .click("继续")
    .end_for()
    .click("返回")
    .run()
```

### 6.2 条件分支 `if_see` / `else_do` / `endif`

```python
auto.do()
    .click("提交")
    .delay(1)
    .if_see("系统提醒")
        .click("我知道了")
    .else_do()
        .click("完成")
    .endif()
    .wait("提交成功")
    .run()
```

### 6.3 整体重试 `.retry()`

整个操作块在失败时自动重试若干次。

```python
auto.do()
    .click("启动流程")
    .retry(count=3, wait=2)       # 从"启动流程"开始，失败就整体重试最多3次
        .click("下一步")
        .wait("加载完成")
    .click("确认")
    .run()
```


## 七、截图与调试（开发者利器）

### 7.1 截图功能一览

| 方法 | 作用 | 示例 |
|:---|:---|:---|
| `auto.shot("名称")` | 全屏截图 | `auto.shot("报表完成")` |
| `auto.snip(x, y, w, h, "名称")` | 区域截图 | `auto.snip(0, 0, 500, 300, "左上角")` |
| `.snap("备注")` | 链式调用中的截图 | `.click("保存").snap("保存后").click("关闭")` |
| `auto.capture("按钮名")` | 交互式模板截图工具 | `auto.capture("新建按钮")` |

### 7.2 自动调试录像

ImgClickFlow **默认开启**操作截图记录。每次操作前后都会自动截图，并保存在以脚本名命名的子文件夹中。

每张截图上自动附加水印：
- **时间戳**
- **操作代码**（如 `.click("保存")`）
- **脚本名**
- **代码行号**

**红框标记**：找到的按钮位置用红框标出。
**蓝框标记**：限定搜索的区域用蓝框标出。

### 7.3 智能截图机制

在 `for_data` 等循环中，大量相同操作的截图会产生冗余。ImgClickFlow 只截取**状态发生变化**的关键帧，避免浪费存储空间，同时保留关键诊断信息。

### 7.4 截图自动清理

默认保留最近 20 张截图，超出自动删除最旧的。避免长期运行后截图文件夹膨胀。

### 7.5 调试开关

```python
auto.debug.on()       # 开启详细追踪
auto.debug.off()      # 关闭追踪
auto.debug.report()   # 生成 HTML 执行报告
auto.debug.replay()   # 控制台回放操作序列
auto.step()           # 切换单步调试模式（每步操作后暂停，等待回车继续）
auto.pace(1.0)        # 设置步骤间默认间隔秒数
```


## 八、环境诊断与工具方法

### 8.1 全面环境诊断 `auto.check()`

一次性检查所有关键配置：

```python
auto.check()
```

输出内容：
- 屏幕分辨率
- DPI 缩放比例
- 依赖库版本
- 模板文件夹路径
- 常见问题预警

### 8.2 模板健康检查 `auto.check_templates()`

逐一验证所有模板截图是否在当前屏幕上可见。

```python
auto.check_templates()
```

适合在更换设备、调整分辨率、系统更新后运行，快速定位失效的模板。

### 8.3 鼠标坐标实时查看 `auto.pos()`

```python
auto.pos()
```

在屏幕上实时显示当前鼠标坐标和 DPI 缩放系数，帮助确定精确坐标用于坐标点击。

### 8.4 帮助文档 `auto.help()`

```python
auto.help()
```

在控制台打印完整的功能清单和用法。


## 九、定时任务

### 9.1 每日定时 `cron`

```python
def my_task():
    auto.click("系统图标")
    auto.click("数据面板")
    auto.shot("每日快照")

auto.cron("09:00", my_task)

import time
while True:
    time.sleep(60)
```

### 9.2 一次性定时 `cron1`

```python
auto.cron1("18:00", my_task)  # 18:00执行一次
```

### 9.3 多定时任务 `timer_tasks`

```python
auto.timer_tasks(
    早间巡检=morning_task,
    午间备份=noon_task,
    晚间报告=evening_task
)
```


## 十、与 Playwright 双剑合璧

ImgClickFlow 负责桌面端一切图形操作，但它不是为 Web 自动化设计的。当你的流程涉及 Web 页面的复杂操作（读取数据、检测元素状态、精确填表），**Playwright** 是更好的选择。

### 职责分工速查

| 场景 | 用什么 |
|:---|:---|
| 桌面软件操作 | ImgClickFlow |
| 远程桌面/虚拟机 | ImgClickFlow |
| 老旧工业软件 | ImgClickFlow |
| Web 页面精确填表 | Playwright |
| Web 页面数据提取 | Playwright |
| Web 页面元素状态判断 | Playwright |
| 混合流程（Web → 桌面） | 两者配合 |

### 协作示例

```python
from playwright.sync_api import sync_playwright
from imgclickflow import auto

# Web 段：Playwright 登录并下载报表
with sync_playwright() as p:
    browser = p.chromium.launch()
    page = browser.new_page()
    page.goto("https://admin.example.com")
    page.fill("input[name='username']", "admin")
    page.fill("input[name='password']", "pass")
    page.click("button[type='submit']")
    page.click("text=下载报表")
    browser.close()

# 桌面段：ImgClickFlow 打开下载的文件并打印
auto.click("文件夹")
auto.write("C:\\下载\\报表.xlsx")
auto.press("enter")
auto.dclick("报表.xlsx")
auto.click_robust("打印")
```

> 详细的 Playwright 搭配策略请参考 **文档二：Python 生态搭配指南**。


## 十一、学习路径建议

| 阶段 | 内容 | 时间 |
|:---|:---|:---|
| 30分钟 | 安装python、vscode | 0.5h |
| 15分钟 | `click`、`write`、`delay`、链式调用 | 0.25h |
| 第1小时 | `click_seq`、`click_any`、`click_robust`、`wait` | 1h |
| 第3小时 | `for_data`、`if_see`、截图调试、环境诊断 | 1h |
| 第4小时 | 定时任务、Playwright 协作 | 1h |

---

> 本文档覆盖了 ImgClickFlow 的全部 GUI 操作能力。下一步请阅读 **文档二：Python 生态搭配指南**，了解如何用 pandas、requests、数据库等模块为自动化脚本注入后台数据处理能力。
```